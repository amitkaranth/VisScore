# Synthetic Tufte / non-Tufte image generation (Diffusers + CUDA).
# Colab usually provides torch with GPU; install the rest with:
#   pip install -r requirements.txt

torch>=2.1.0
diffusers>=0.32.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
Pillow>=9.0.0
