"""
VisScore core package.

This repo is intentionally runnable without installing a wheel:
entrypoints (Streamlit/CLI) add the repo's `src/` directory to `sys.path`.
"""

__all__ = ["inference", "vlm"]

